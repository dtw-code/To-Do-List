name: expense_tracker
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  uuid: ^4.5.1
  intl: ^0.20.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1    # 👈 Correct placement

# ----------------------------------------------------------
# 👇 MUST be at top level (aligned with flutter/dev_dependencies)
# ----------------------------------------------------------
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon_expense.png"
# ----------------------------------------------------------

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
